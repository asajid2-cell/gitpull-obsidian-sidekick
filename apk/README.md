# APK

Install `gitpull-debug.apk` by transferring it to an Android device and opening it, or with ADB:

```powershell
adb install -r apk/gitpull-debug.apk
```

Expected SHA-256:

```text
d07d545e76cc46e8579c15b0c97977128971b579a0ea500ed2101f09dca306f7
```
